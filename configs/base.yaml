# ML evaluation & metrics (обязательно для F1, strict_span_f1)
scikit-learn>=1.2.0
seqeval>=1.2.2  # Для NER-specific метрик

# Datasets (для загрузки/обработки train_dataset.tsv)
datasets>=2.14.0

# Accelerate (оптимизация обучения на GPU)
accelerate>=0.21.0

# Опционально: wandb для логирования экспериментов
# wandb>=0.15.0

# Опционально: для data augmentation (если включите)
nlpaug>=1.1.11
